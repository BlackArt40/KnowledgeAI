# 📁 项目结构

> 本文档说明 KnowledgeAI 的目录组织，帮助新成员快速定位代码。

## 顶层目录

```
KnowledgeAI/
├── src/                  # 应用源码（App Router 页面 + API + 业务库）
├── prisma/               # 数据库 schema + 迁移 + 种子数据
├── scripts/              # 运维脚本（cron/迁移/清理）
│   └── smoke/            # 手动冒烟测试脚本（npx tsx 运行）
├── tests/                # 集成测试（需启动 dev server，用 node 运行）
├── docs/                 # 产品文档 / 设计说明 / 路线图 / 截图
├── public/               # 静态资源（favicon、SVG 图标）
├── .github/workflows/    # CI 流水线（tsc + prisma drift + build）
├── .tessdata/            # Tesseract OCR 语言包（首次 OCR 时下载，gitignored）
└── .uploads/             # 本地上传文件存储（demo 模式，gitignore）
```

## src/ — 应用源码

```
src/
├── app/                          # Next.js App Router
│   ├── (app)/                    # 工作区页面（AppShell 包裹，需登录）
│   │   ├── dashboard/            # 仪表盘
│   │   ├── knowledge-base/       # 知识库管理 + 文档列表
│   │   ├── chat/                 # RAG 智能问答
│   │   ├── agent/                # Agent 调研
│   │   ├── team/                 # 团队协作
│   │   ├── billing/              # 订阅计费
│   │   ├── settings/             # 用户设置 / 安全 / 模型配置
│   │   └── admin/                # 管理后台（Owner/Admin）
│   ├── (auth)/                   # 认证页面（登录 / 注册 / 验证邮箱）
│   ├── api/                      # API Route Handlers
│   │   ├── auth/                 # 登录 / 注册 / me
│   │   ├── knowledge-base/       # KB CRUD + 文档上传 / 删除
│   │   ├── upload/chunk/         # 分片上传（init / chunk / status / complete）
│   │   ├── chat/                 # SSE 流式问答
│   │   ├── agent/                # Agent 调研 SSE + 任务查询
│   │   ├── team/                 # 团队 / 成员 / 审计 / KB 权限
│   │   ├── billing/              # 套餐 / 订单 / webhook
│   │   ├── models/               # 外部 LLM 配置 / 测试 / 切换
│   │   ├── notifications/        # 通知收件箱 / 偏好
│   │   ├── security/             # 2FA / 会话 / 隐私 / GDPR 导出
│   │   ├── admin/                # 用户管理 / KB 监控 / 系统配置 / 清理
│   │   └── usage/                # 用量统计
│   ├── privacy/ terms/           # 静态法律页面
│   ├── r/[id]/                   # Agent 报告公开分享页
│   ├── maintenance/              # 维护模式页
│   ├── layout.tsx                # 根布局（字体 / 主题）
│   ├── globals.css               # 全局样式 + CSS 变量
│   ├── error.tsx not-found.tsx   # 错误 / 404 边界
│   └── page.tsx                  # 落地页
│
├── components/                   # React 组件
│   ├── ui/                       # 基础组件库（Button/Input/Dialog 等 15 个）
│   ├── app/                      # 业务组件（按模块分子目录）
│   │   ├── kb/                   # 知识库相关（UploadZone / 文档卡片等）
│   │   ├── chat/                 # 问答相关（消息流 / 引用面板）
│   │   └── ...
│   ├── marketing/                # 营销页组件（Hero / Feature）
│   ├── icons/                    # 自绘品牌图标
│   ├── logo.tsx                  # 品牌 Logo
│   └── theme-toggle.tsx          # 亮 / 暗主题切换
│
├── lib/                          # 业务逻辑层（无 React 依赖）
│   ├── auth/                     # JWT 鉴权 + RBAC 四角色 + 用户存储
│   ├── kb/                       # 知识库 + 文档 CRUD + 处理管线触发
│   ├── rag/                      # RAG 引擎
│   │   ├── parser.ts             # 多格式解析（PDF/Word/Excel/PPT/HTML/OCR）
│   │   ├── ocr.ts                # Tesseract.js OCR（语言包在 .tessdata/）
│   │   ├── chunker.ts            # 智能切片（标题感知 / 表格代码保护）
│   │   ├── embeddings.ts         # 本地哈希嵌入（fallback）
│   │   ├── vector-store-*.ts     # 向量库适配器（memory/pgvector/chromadb/pinecone）
│   │   ├── bm25.ts               # BM25 关键词检索
│   │   ├── hybrid-search.ts      # 混合检索（向量 + BM25 + Reranking）
│   │   ├── retriever.ts          # 检索入口
│   │   ├── generator.ts          # LLM 生成 + 引用组装
│   │   ├── indexer.ts            # 文档索引（解析→切片→嵌入→入库）
│   │   └── conversation-context.ts # 对话上下文管理
│   ├── agent/                    # Agent 调研编排（规划→检索→分析→撰写）
│   ├── chat/                     # 对话存储
│   ├── team/                     # 团队 / 成员 / 审计日志 / KB 权限
│   ├── billing/                  # 订阅 / 订单 / 发票 / 用量计量
│   ├── models/                   # 外部 LLM Provider 配置（per-user 上下文）
│   ├── llm/                      # LLM Provider 抽象（OpenAI/DeepSeek/...）
│   ├── notifications/            # 通知中心（4 通道 + 偏好）
│   ├── security/                 # 2FA(TOTP) / 限流 / 会话 / 隐私
│   ├── apikeys/                  # API Key 管理
│   ├── admin/                    # 管理后台数据（用户 / KB 监控 / 系统配置）
│   ├── upload/                   # 分片上传会话管理 + 前端 hook
│   ├── storage/                  # 文件存储（本地 / S3）+ 清理任务
│   ├── queue/                    # 异步任务队列（Memory / BullMQ+Redis）
│   ├── db/                       # Prisma 数据库层（client / hydrate / persist）
│   ├── config.ts                 # Provider 状态聚合（管理面板数据源）
│   ├── format.ts                 # 格式化工具
│   └── utils.ts                  # 通用工具
│
├── types/                        # 全局类型定义
└── proxy.ts                      # 中间件（限流 + DB 水合触发 + 清理定时器）
```

## 根目录关键文件

| 文件 | 作用 |
|------|------|
| `next.config.ts` | Next.js 配置（standalone 输出 / serverExternalPackages / 安全头） |
| `tsconfig.json` | TypeScript 配置（strict / `@/*` 路径别名） |
| `package.json` | 依赖 + 脚本（pnpm@11.7.0 / Node 22） |
| `pnpm-workspace.yaml` | 原生构建白名单（prisma engines / esbuild / sharp） |
| `instrumentation.ts` + `instrumentation-node.ts` | 服务器启动钩子（启动队列 worker） |
| `worker.ts` | 独立 Worker 进程入口（Docker worker 服务） |
| `proxy.ts`（在 `src/`） | Next.js 16 中间件（原 middleware.ts） |
| `Dockerfile` | 多阶段构建（deps → build → minimal runner） |
| `docker-compose.yml` | 编排：app + worker + Redis |
| `AGENTS.md` | AI 编辑会话指南（命令 / 架构 / 约定） |
| `.env.example` | 环境变量参考（含 demo fallback 文档） |

## scripts/ — 运维脚本

```
scripts/
├── check-prisma-migrations.sh   # CI 迁移漂移检查（shell）
├── cleanup-temp-files.ts        # 临时文件清理（可配 cron）
├── migrate-vector-store.ts      # 内存索引批量迁移到向量库
└── smoke/                       # 手动冒烟测试（npx tsx scripts/smoke/<name>.ts）
    ├── test-parser.ts           # 多格式文档解析
    ├── test-ocr-*.ts            # OCR 图片 / 扫描 PDF
    ├── test-chunker.ts          # 智能切片
    ├── test-hybrid-search.ts    # 混合检索
    ├── test-chromadb.ts         # ChromaDB 适配器
    ├── test-pinecone.ts         # Pinecone 适配器
    ├── test-persistence.ts      # DB 持久化（写穿 + 水合）
    ├── test-fallback.ts         # 无 DB 时内存回退
    ├── test-chunked-upload.ts   # 分片上传 + 断点续传
    ├── test-cleanup.ts          # 临时文件清理
    ├── test-routing.ts          # 路由权限
    ├── test-strip-html.ts       # HTML 清洗
    ├── test-is-scanned.ts       # 扫描件检测
    ├── mock-pinecone-server.ts  # Pinecone mock 服务器
    ├── test-agent-graph.ts      # P2-1 StateGraph DAG 引擎验收
    ├── test-external-sources.ts # P2-2 外部数据源验收
    ├── test-report-enhance.ts   # P2-3 报告增强验收（导出/分享/版本/评论）
    └── verify-imports.ts        # 依赖导入验证
```

## tests/ — 集成测试

```
tests/
├── api/api-test.mjs             # 38 个 API 路由（含 SSE）
├── functional/functional-test.mjs  # 25 个页面 / 流程检查
└── performance/performance-test.mjs  # 性能 + 限流
```

> 需先启动 `pnpm dev`，再用 `node tests/<suite>/<suite>-test.mjs` 运行。非单元测试框架，是冒烟 / 集成检查。

## prisma/ — 数据库

```
prisma/
├── schema.prisma                # PostgreSQL schema（模型 / 枚举 / 关系）
├── migrations/                  # 迁移文件（CI 检查漂移）
│   └── 20260713022119_init/     # 初始迁移
└── seed.ts                      # 演示数据（4 用户 + 5 KB + 1 团队）
```
