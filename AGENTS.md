# AGENTS.md

Compact guidance for OpenCode sessions working in this repo. Read this before editing.

## Commands

```bash
pnpm install              # install (uses pnpm@11.7.0, Node 22)
pnpm dev                  # dev server on :3000
pnpm build                # production build (output: "standalone")
pnpm lint                 # ESLint (next core-web-vitals + TS). MUST exit 0 before pushing.
pnpm test                 # unit + integration + e2e (integration needs a live dev server)
pnpm test:unit            # vitest run --coverage (coverage thresholds are a hard gate)
pnpm test:integration     # tests/ suites against a live dev server (start it first)
pnpm test:e2e             # Playwright (auto-starts pnpm dev via webServer)
npx tsc --noEmit          # typecheck - this is what CI runs, not a script
```

### CI gate (`.github/workflows/ci.yml` - four jobs, all must pass)

1. `quality` (postgres service): `npx prisma generate` → `npx tsc --noEmit` → `prisma migrate diff --exit-code` (fails if `schema.prisma` drifted from migrations) → `pnpm lint` → `pnpm build`
2. `unit`: `pnpm test:unit` - vitest with **coverage thresholds** (lines/functions/statements 70%, branches 60% on `src/lib/{rag,auth,billing,team}`) - below threshold = red
3. `integration`: starts `pnpm dev` in demo mode (high rate-limit envs) → `tests/` functional + api + performance
4. `e2e`: `playwright install --with-deps chromium` → `pnpm test:e2e`

So: run `pnpm lint` + `pnpm test:unit` locally before pushing, and never edit `prisma/schema.prisma` without running `npx prisma migrate dev --name <descriptive>` to generate a matching migration. Migrations: `20260713022119_init` + P3-4 audit + P5-3/5-4/5-5 + P7-1 webhooks + P7-2 bots + P7-3 knowledge graph (hand-written SQL, drift-checked against schema). PR merge gate = branch protection requiring the four checks (GitHub settings side; docs/ROADMAP notes the steps).

### Tests

`tests/<suite>/<suite>-test.mjs` are standalone Node scripts that hit a **live** `pnpm dev` server (they log in with demo accounts - the API routes require auth). Start the dev server first, then:

```bash
node tests/functional/functional-test.mjs    # ~5s, 25 page/flow checks
node tests/api/api-test.mjs                  # ~15s, 38 API routes incl. SSE
node tests/performance/performance-test.mjs  # ~120s, rate-limit window waits
```

Each script writes a Chinese-named Markdown report next to itself.

**Unit tests (P6-3)**: vitest under `src/lib/**/*.test.ts` (`vitest.config.ts`, coverage thresholds). New lib code in `src/lib/{rag,auth,billing,team}` SHOULD come with a test file - CI's unit job fails below 70% coverage. Pure functions + globalThis stores are the easy targets; LLM paths are tested via `vi.mock("@/lib/llm/provider")`; heavy deps (pdf-parse/mammoth/xlsx) via `vi.mock`. Coverage excludes external-service backends (chromadb/pgvector/pinecone) + ocr.ts (tesseract binaries) - documented in ROADMAP.

**E2E (P6-3)**: Playwright under `e2e/` (`playwright.config.ts`, webServer auto-starts dev, `locale: "zh-CN"`). `e2e/main-flow.spec.ts` covers login → upload → Q&A → Agent. Controlled inputs need a fill-verify-retry loop (React hydration race).

`scripts/smoke/test-*.ts` and `scripts/smoke/mock-pinecone-server.ts` are manual smoke scripts run via `npx tsx scripts/smoke/<name>.ts`; not part of any suite. Operational scripts (`scripts/cleanup-temp-files.ts`, `scripts/migrate-vector-store.ts`, `scripts/check-prisma-migrations.sh`) stay at `scripts/` root. Deploy helpers live in `scripts/deploy/` (staging.sh, blue-green.sh) - invoked by `.github/workflows/deploy*.yml` over SSH.

## Architecture: in-memory store + write-through DB (critical)

Reads always come from in-memory stores on `globalThis` (`__KAI_USER_STORE__`, `__KAI_KB_STORE__`, `__KAI_CHAT_STORE__`, `__KAI_AGENT_STORE__`, `__KAI_MODEL_STORE__`, `__KAI_NOTIF_STORE__`, `__KAI_TEAM_STORE__`, `__KAI_ADMIN_STORE__`, `__KAI_QUEUE_INSTANCE__`). PostgreSQL is a **persistence layer**, not the read path.

- `src/lib/*/store.ts` - the in-memory stores (source of truth for reads)
- `src/lib/db/hydrate.ts` - on first API request, loads DB rows into the stores (lazy, once)
- `src/lib/db/persist.ts` - fire-and-forget write-through on mutations (errors logged, not thrown)
- `src/lib/db/client.ts` - lazy Prisma singleton; returns `null` if `DATABASE_URL` unset or `@prisma/client` missing

**To add a new persisted entity you must touch all of:** store.ts (in-memory shape) -> persist.ts (write-through fn) -> hydrate.ts (load fn) -> `prisma/schema.prisma` -> new migration. Forgetting any one leaves the DB and memory out of sync.

When `DATABASE_URL` is unset, the app runs in pure demo mode (memory only, reset on restart). This is the default - don't assume a DB is present.

## Background job queue (P0-4)

Document processing and agent research run in a background queue, not in the request thread. Two backends, selected by `REDIS_URL`:
- **Memory** (default) - in-process, no persistence, `instrumentation-node.ts` starts it on boot
- **BullMQ + Redis** - multi-instance, retries, dead-letter queue, separate worker process

Key files:
- `src/lib/queue/index.ts` - factory + agent event bus (publish/subscribe for SSE relay)
- `src/lib/queue/handlers.ts` - `doc-process` (parse->chunk->index), `agent-run` (runTask + event publishing), `index-cleanup`
- `src/lib/queue/memory-queue.ts` - demo backend with retry (3 attempts, exponential backoff)
- `src/lib/queue/bullmq-queue.ts` - Redis backend
- `src/lib/queue/agent-bus-redis.ts` - Redis Pub/Sub for cross-process agent event relay
- `instrumentation.ts` + `instrumentation-node.ts` - server boot hook that starts the worker (split because Edge Runtime rejects `process.on`/`process.exit`)
- `worker.ts` - standalone worker process entrypoint (Docker `worker` service)

**Agent SSE flow**: `/api/agent/run` enqueues `agent-run`, then opens SSE subscribing to the agent event bus. The worker runs `runTask` and publishes `step`/`done`/`error`/`end` events. In memory mode this is an EventEmitter; with Redis it's Pub/Sub so the worker can run in a separate process. Event names (`init`/`step`/`done`) are asserted by tests - don't rename them.

**Adding a new job type**: add to `JobType` union in `interface.ts` -> write handler in `handlers.ts` -> register in `registerAllHandlers()` -> enqueue from the route handler. The MemoryQueue auto-starts on first enqueue (HMR-safe via `globalThis.__KAI_QUEUE_INSTANCE__`).

## Middleware is `src/proxy.ts`, not `middleware.ts`

Next.js 16 renamed the middleware file to `proxy.ts`. It does two things:

1. Triggers `ensureHydrated()` (fire-and-forget) on first non-`/_next` request.
2. Rate-limits `/api/*` (Redis sliding window if `REDIS_URL`, else in-memory) with **tiered limits** (P3-3): one dimension per request — anonymous `ip:<ip>` (`RATE_LIMIT_ANON_PER_MIN`, default 20), JWT user `user:<userId>` (`RATE_LIMIT_PER_MIN`), API key `apikey:<keyId>` (`RATE_LIMIT_KEY_PER_MIN`, default 500). The per-KB tier (`kb:<kbId>`, `RATE_LIMIT_KB_PER_MIN`, default 60 — keep below the user limit) runs in route handlers because the proxy can't read bodies/path params: `/api/chat` (also covers the SSE skip) and `/api/knowledge-base/[id]` `loadAccessible`. 429s carry `dimension` in the body and an accurate `Retry-After` (Redis resetAt = earliest ZSET entry + window). Allowed responses intentionally carry no `X-RateLimit-*` headers (Next.js proxy header merge would override route-level 429 headers).

`SKIP_PATHS` excludes SSE streams and high-frequency polls from rate limiting: `/api/chat`, `/api/agent/run`, `/api/billing/webhook`, `/api/notifications`, `/api/auth/me`. Add new SSE/poll endpoints here or they'll get 429'd.

## Provider fallback pattern

Every external dependency has an env-gated real implementation plus a demo fallback. Gate checks live in each module's `provider.ts` and are aggregated by `src/lib/config.ts` (used by the `/admin` status panel). Modules: LLM (`src/lib/llm`), embedding (`src/lib/rag`), external data sources (`src/lib/external`), DB, storage (`src/lib/storage`), payment (`src/lib/billing`), rate-limit, queue (`src/lib/queue`).

When adding a new external integration, follow the same shape: env check -> real impl -> fallback -> register in `getProviderStatus()`.

## Prisma specifics

- `@prisma/client` **is** a runtime dependency (despite the README saying `pnpm add` to enable). It's lazy-loaded only when `DATABASE_URL` is set. Listed in `next.config.ts` `serverExternalPackages`.
- `prisma/seed.ts` is `@ts-nocheck` - edits there won't break `tsc --noEmit`, but also won't get type checking.
- pgvector: the `KbChunk` model exists in `schema.prisma` but the `embedding` column is added via **raw SQL** (Prisma can't model the `vector` type). Requires `CREATE EXTENSION IF NOT EXISTS vector;` on the DB. See `src/lib/rag` vector store implementations (`memory` | `pgvector` | `chromadb` | `pinecone`).
- Seed demo data: `npx prisma db seed` (configured in `package.json` -> `prisma.seed`). Creates 4 demo users (password `password123`) + 5 KBs + 1 team.

## Conventions

- **技术选型原则：不造重复轮子** —— 优先采用成熟、维护活跃且与当前技术栈（Next 16 / React 19 / Node 22 / Edge runtime）兼容的现成框架与库（例：Auth.js v5 做 OAuth、BullMQ 做队列、Prisma 做持久化、Radix UI 做无障碍原语、pino 做日志、pdf-parse/mammoth/xlsx 做解析、swagger-ui-dist 做 API 文档）。**只有「没有合适的现成方案」或「现成方案与项目约束冲突」（兼容性风险、依赖过重、集成成本高于自研）时才自研**，且自研必须在 ROADMAP / 设计说明注明替代对象与理由（P7-5 已把大部分自研组件换成成熟框架：jose 做 JWT、otplib 做 TOTP、react-markdown+remark-gfm+rehype-highlight 做 Markdown 渲染、d3-force 做图谱布局、miniSearch 做 BM25、i18next 做 i18n、Vercel AI SDK 做 LLM 客户端——详见 docs/设计说明.md 十五·二十二；**维持自研**的只剩监控/追踪 OTel/Sentry SDK——与 Edge proxy 集成成本高，及 PBKDF2/AES——crypto 标准算法薄封装）。选型流程：先调研成熟方案 → 验证与 Next 16/Edge 兼容 → 确认不适用再自研，不要凭「零依赖惯例」默认造轮子。
- **TypeScript strict**, path alias `@/*` -> `./src/*`. No `as any` / `@ts-ignore` in app code (the `as unknown as` casts in `persist.ts`/`hydrate.ts` are a known smell for models not in the generated client types - match the existing pattern there rather than introducing `as any`).
- App Router under `src/app/`: route groups `(app)` (AppShell-wrapped workspace), `(auth)` (login/register/verify), plus top-level `api/`, `privacy/`, `terms/`, `maintenance/`.
- RBAC roles: `OWNER` / `ADMIN` / `EDITOR` / `VIEWER`. Guard via `src/lib/auth/guard.ts`. Self-registration defaults to `EDITOR`.
- SSE routes: `/api/chat` emits `sources` / `token` / `done` (`sources` carries `url`/`sourceType` when 联网搜索 is on); `/api/agent/run` emits `init` / `step` / `done` / `error` / `end`. Tests assert these event names - don't rename them.
- Not a monorepo: `pnpm-workspace.yaml` is only an `allowBuilds` allowlist for native builds (prisma engines, esbuild, sharp, etc.). Single package.
- Docker: `output: "standalone"` build copied into a minimal `node:22-alpine` runner. `docker compose up -d` brings up app + worker + Redis (port 6380).

## Demo logins (for manual QA)

All four share password `password123`:

| Email | Role |
| --- | --- |
| owner@knowledgeai.dev | OWNER |
| admin@knowledgeai.dev | ADMIN |
| editor@knowledgeai.dev | EDITOR |
| viewer@knowledgeai.dev | VIEWER |

## Key files to know

- `src/proxy.ts` - middleware (rate limit + hydration trigger)
- `instrumentation.ts` + `instrumentation-node.ts` - server boot hook (starts queue worker)
- `src/lib/config.ts` - provider status aggregator (admin panel source of truth)
- `src/lib/queue/` - background job queue (doc-process, agent-run, index-cleanup)
- `src/lib/external/` - external data sources (web search Tavily/SerpAPI/Brave + ArXiv + GitHub; used by agent research and the chat 联网搜索 toggle)
- `src/lib/db/{client,hydrate,persist}.ts` - the DB adaptation triad
- `src/lib/auth/guard.ts` - RBAC role guard for route handlers
- `src/lib/crypto.ts` - AES-256-GCM + HKDF (keyed by `AUTH_SECRET`); `encryptToString`/`decryptFromString`/`isEncrypted`. API key secrets and model keys are stored ENCRYPTED in the DB (P3-4) - never write a plaintext secret to a new persist/hydrate path. Sessions: `src/lib/auth/session.ts` mints HS256 JWTs via **jose** (P7-5; `SignJWT`/`jwtVerify`/`base64url` - old-format tokens still verify, wire-compat tested); 2FA codes via **otplib** `src/lib/security/totp.ts` (epoch in seconds, `epochTolerance` in seconds - ±1 step = ±30s; RFC 6238 vectors tested; backup codes stay hand-rolled SHA-256).
- `src/lib/security/audit.ts` - global security audit trail (P3-4): `recordAudit` appends a HMAC-SHA256 hash-chained entry (tamper-evident), `listAudit` filters by action/actor/time, `verifyAuditChain` reports chain integrity, `trimAudit` enforces `AUDIT_RETENTION_DAYS`. Sensitive ops (login, deletes, permission changes, exports, config) call `recordAudit` - new sensitive routes should too. Query via `GET /api/admin/audit`.
- `src/lib/realtime/` - realtime collaboration (P4-1): `bus.ts` (channel pub/sub on globalThis; publish `kb:<kbId>` / `presence` / `conv:<id>` from store mutations) + `presence.ts` (SSE-connection == online). SSE endpoints: `/api/kb/[id]/events`, `/api/team/presence/events`, `/api/chat/conversations/[id]/events` (all in proxy `SKIP_PATHS`, with per-KB tier enforced in-route where needed; 30s `: ping` frames). KB settings PATCH uses optimistic concurrency (`baseVersion` -> 409). Frontend consumption via `useSse` hook / `PresenceProvider`.
- `src/lib/kb/doc-share.ts` - document share links (P4-2): one link per doc (expiry / SHA-256 password / maxViews / revoke), `checkDocShare` returns 410/401/403/404 codes; public endpoint `GET /api/share/doc/[token]` (in proxy `SKIP_PATHS` - the token is the credential).
- KB permissions (P4-2): `KbDocument.access` (view/edit/private) overrides KB-level `kbAccess` via `canViewDoc`/`canEditDoc` (kb/store); per-KB member roles `kbMemberRoles` keyed by member email override `canViewKb`/`canEditKb` internally (team/store, signatures unchanged - keep them unchanged so all call sites stay working). Sensitive permission/share changes must call `recordAudit` (`doc.access_change`, `kb.access_change`, `sharelink.create/revoke`).
- `src/lib/workspace/store.ts` - multi-tenant workspaces (P4-3): `Workspace` (id/name/plan/ownerId/members-by-email), default `ws_default` holds all seed users. `RequestUser.workspaceId` is resolved from the `kai-workspace` cookie in `getRequestUser` (membership-checked, falls back to default). KB / Conversation / AgentTask carry `workspaceId`; list/detail routes scope by it - new read routes for tenant data MUST filter by `u.workspaceId` or a cross-tenant leak happens. Usage metering is per-workspace (`usageByWorkspace`, `recordQa(userId, workspaceId)` / `recordAgentTask`); workspace plan upgrades via checkout.
- Mobile / PWA (P5-1): `src/components/ui/sheet.tsx` - Radix-Dialog-based drawer (scroll lock / Escape / focus trap / enter-exit animation) used by the AppShell nav and the chat page's conversation + sources drawers. `src/hooks/use-media-query.ts` - SSR-safe `useIsMobile()` via `useSyncExternalStore` (don't reimplement with an effect + setState; the linter rejects it). `src/hooks/use-gestures.ts` - edge-swipe / horizontal-swipe / long-press (native listeners; long-press uses non-passive click/contextmenu suppression). Hand-written PWA (zero deps): `public/sw.js` (precache app shell + `/_next/static` stale-while-revalidate + navigations network-first with cache fallback + `/api` never cached; bump `VERSION` on deploy), `public/manifest.webmanifest`, icons in `public/icons/` (regenerate via `npx tsx scripts/generate-pwa-icons.ts`), registered only in production by `src/components/pwa/sw-register.tsx`. AppShell auth: only a 401 redirects to /login - network failures keep the shell mounted so offline users see cached content. The chat page height uses the `.chat-height` CSS class (100dvh with 100vh fallback) in `globals.css`, not a Tailwind arbitrary value (fallback ordering matters).
- Global search (P5-2): `GET /api/search?q=` returns all five entity categories in one request (KB / docs / conversations / agent tasks / settings). It MUST stay workspace-scoped (`listAllKbs(u.workspaceId)`, `canViewDoc` for docs, `listTasks(u.id, u.workspaceId)`) and role-filter settings entries - new entity types added to the store should be added here too. `src/components/app/global-search.tsx` is the Cmd+K panel (Radix Dialog; container-level keydown for ↑/↓/Enter navigation; `kai-recent-search` localStorage). `src/hooks/use-global-hotkey.ts` binds Cmd/Ctrl+K. Deep links: `/chat?kb=&conv=`, `/agent?task=`, `/settings?tab=security|profile|privacy|models`, `/knowledge-base?new=1` - keep them working when pages change. Note: `/api/agent/tasks` must filter by workspaceId (was fixed in P5-2 - don't regress it to `listTasks(u.id)` only).
- Chat experience (P5-3): `src/components/app/chat-markdown.tsx` - react-markdown + remark-gfm + rehype-highlight renderer (P7-5 replaced the hand-written tokenizer; `hljs-*` token classes themed in globals.css; `[n]` citation chips via a rehype plugin -> `<button data-cite>` + container onClick delegation; copy button; mermaid `graph LR` chip flow preserved with the `-->(?!>)` edge regex so `-->>` sequence arrows don't match). Message feedback loop: `ChatMessage.feedback/feedbackNote/feedbackAt` persisted via `persistMessageFeedback` (per-message upsert - `persistConversation` only writes the LAST message, don't rely on it for historical messages); `POST /api/chat/conversations/[id]/messages/[mid]/feedback` targets the SERVER message id (`serverId` on the client Msg, assigned on SSE `done` - the local id is a placeholder); `/api/chat` down-weights chunks cited by `feedback==="down"` messages in the same conversation (`score × 0.4`). Regenerate: `{ regenerate, temperature, topK }` body pops the last assistant message server-side (memory + DB via `popLastAssistantMessage`/`deleteMessageFromDb`) and does NOT re-add the user message. Archive/tags: `Conversation.archived/tags` (persisted with `shared`/`workspaceId` - those used to be memory-only and lost on restart); list API hides archived by default (`?archived=1` to list them). Conversation menus use the ⋯ button (desktop hover / mobile long-press via `useLongPress`). KB recommendations: `GET /api/knowledge-base/recommend?q=&excludeKbId=` (2-gram overlap scoring, workspace-scoped).
- `src/lib/rag/parser.ts` - multi-format document parser (PDF/Word/Excel/PPT/HTML/MD/TXT/CSV + image via OCR)
- `src/lib/rag/ocr.ts` - OCR pipeline (tesseract.js + pdfjs-dist + @napi-rs/canvas; env: `OCR_ENABLED`/`OCR_LANG`/`OCR_MAX_PAGES`; `.tessdata/` caches language packs, gitignored)
- i18n (P5-4): `src/lib/i18n/` - i18next v26 backed (P7-5 replaced the hand-rolled resolver; next-intl was rejected - it sits in a Next 16.3 API migration window, while i18next is framework-agnostic). `translate.ts` (i18next instance + `getFixedT`; custom `{`/`}` interpolation prefix; `escapeValue:false` - React escapes text nodes, avoid double-escaping; missing key -> key, missing var -> placeholder kept so n8n `{{ $json.question }}` templates survive), `messages/zh-CN.json` + `en.json` (685 keys, parity asserted), `provider.tsx` (`LocaleProvider` + `useT`/`useLocale`; SSR locale comes from the `kai-locale` cookie via `serverLocale`, hydration prefers localStorage; `setLocale` writes localStorage + cookie + `<html lang>`), `server.ts` (`serverT()` for server components / metadata), `use-format.ts` (locale-aware formatRelative/formatDate/formatNumber). New UI strings MUST go through `t()` - `scripts/smoke/test-i18n-coverage.ts` fails the build on any residual Chinese in `src/app` + `src/components` (multi-line JSX text nodes are caught too). Server components use `await serverT()` (never `useT`); module-level arrays with copy use factory functions taking `t`. Language packs are extracted with `scripts/tools/i18n-extract.py`. `User.locale` persists the preference (PATCH /api/auth/me).
- Theme (P5-5): `src/lib/theme/` - three-mode engine + brand colors. `mode.ts`: `kai-theme` = `system|light|dark` (default system; missing = follow OS), `applyTheme/applyHighContrast/getStoredTheme`, `theme-transition` class drives the switch cross-fade (~500ms, disabled under `prefers-reduced-motion`); the pre-hydration inline script in `src/app/layout.tsx` MUST stay in sync (it applies `.dark`/`.high-contrast` from `kai-theme`/`kai-hc` before paint). `brand-colors.ts`: 6 curated workspace colors (indigo/emerald/sky/violet/fuchsia/rose), each with light/dark/high-contrast HSL var sets; `getBrandCss(id)` (pure, SSR-safe) + `applyBrandColor(id)` (client style-tag swap). High contrast: `.high-contrast` class + `@media (prefers-contrast: more)` in globals.css (text ≥4.5:1, borders ≥3:1 - `scripts/smoke/test-theme-ui.mjs` asserts computed ratios). Workspace brand color: `Workspace.brandColor` on the prisma `Workspace` model (migration `p5_5_workspace_brand_color`) persisted via `persistWorkspace`/`hydrateWorkspace` (members stay memory-only - DB rows hydrate name/plan/ownerId/brandColor only); `PATCH /api/workspaces` is owner-only (403) + palette-validated (400) + audited (`workspace.update`); SSR injection happens in `src/app/(app)/layout.tsx` (`<style id="kai-brand-style">`). New theme UI strings go through `t()` (settings page.settings.s72「外观」tab -> `src/components/app/theme-settings.tsx`; `page.theme-settings.*` + `page.theme-toggle.s1-3` keys).
- Observability (P6-1): `src/lib/obs/` - zero-dependency monitoring (OTel/Sentry SDKs intentionally NOT used - documented in ROADMAP/设计说明). `metrics.ts` (Edge-safe, pure globalThis `__KAI_OBS_STORE__`): request SLIs + per-minute series + latency ring (1000) + RAG/LLM/DOC/AGENT dimensions + `MODEL_COST_1K` LLM cost table; `recordRequest/recordRag/recordLlm/recordDoc/recordAgent` + `getMetricsSnapshot()`. `context.ts` + `trace.ts` (AsyncLocalStorage): `withApiTrace(req, name, fn, {autoEnd})` wraps route handlers (api root span + status-aware SLI - middleware CANNOT observe downstream responses in Next 16, so request metrics live in routes), `runWithTraceId` for queue roots, `withSpan`/`traceBegin`+`traceEnd` for lib code (no-op without context); spans ring-capped at 300, `X-Trace-Id` header propagation (proxy injects it). `errors.ts`: error ring (200) + `reportError` + zero-dep Sentry envelope POST (`SENTRY_DSN`-gated, `parseDsn/buildEnvelope/sendToSentry`). Instrumented routes: /api/chat (SSE - `autoEnd:false`, finalize in stream finally), /api/search, /api/agent/run, /api/workspaces, /api/auth/me, /api/knowledge-base(+upload), /api/agent/tasks, /api/chat/conversations(+[id]). Queue jobs carry `payload.traceId` (upload route: `startProcessing` reads it from ALS via `currentTraceCtx`). New routes touching RAG/LLM should reuse `withSpan`/`recordLlm` - the LLM provider already records usage/tokens/cost internally (don't double-record). Dashboard: `/admin/monitoring` (owner/admin, `GET /api/admin/monitoring` + `/api/admin/monitoring/traces?id=`), nav key `page.app-shell.s27`, UI strings `page.monitoring.*`. Frontend errors: `src/components/obs/error-reporter.tsx` (root layout) + `error.tsx` -> `POST /api/obs/report`. Tests: `test-monitoring.ts` (HTTP), `test-sentry.ts` (lib + local receiver), `test-monitoring-ui.mjs` (CDP).
- Structured logging (P6-2): pino 10 singleton in `src/lib/obs/log.ts` (Node only) - EVERY log line is one JSON object on stdout; `LOG_LEVEL` (debug|info|warn|error, default info) + `setLogLevel()`; `mixin()` auto-attaches `requestId` == the ALS trace id (== X-Trace-Id) to every line inside a traced request - never pass a plaintext secret as a log field; pino `redact` censors a built-in sensitive-key table (apiKey/password/token/secret/authorization/cookie/... with exact + 1/2-level wildcard paths - fast-redact has NO `**` deep wildcard, verified against pino 10) + `LOG_REDACT_KEYS` extra paths; free-text values (provider error bodies etc.) MUST go through `redactText()` (`src/lib/obs/redact.ts`, pure Web-API, also used by log-edge/log-browser). Loki aggregation: `LOG_LOKI_URL` gated batch push (`POST /loki/api/v1/push`, 2s/100 lines, lazily-read env - unset = stdout only, zero network; failures dropped, never thrown; `flushLogs()` for tests). Recent lines kept in `__KAI_LOG_STORE__` ring (1000) served by `GET /api/admin/logs?level=&requestId=&limit=` (owner/admin). Per-request lines: proxy emits `http.request` via `src/lib/obs/log-edge.ts` (Edge-safe console JSON - the ONLY file allowed to call console.*, asserted by `test-logging.ts`; SKIP_PATHS requests skip it), `trace.ts finalizeRecord` emits `http.response` (method/path/status/durationMs) / `queue.finish`. Client: `src/lib/obs/log-browser.ts` (pino/browser, no redact - client sites pass status/messages only). RULES: never add a new `console.*` call to server code (test-logging.ts static-scan fails the build); keep the `[module]` prefix in the message; put error objects in the `err` field (pino err serializer -> {type,message,stack}). Tests: `test-logging.ts` (HTTP + static scan), `test-logging-sink.ts` (lib + local receiver).
- CI/CD (P6-3): `.github/workflows/ci.yml` - four jobs (quality/unit/integration/e2e, see the CI gate section above). `vitest.config.ts` - unit tests for `src/lib/{rag,auth,billing,team}` with coverage thresholds (excludes external-service backends + ocr.ts, documented). `playwright.config.ts` + `e2e/main-flow.spec.ts` - Playwright key-user-flow E2E (`locale: "zh-CN"`; webServer auto-starts `pnpm dev`; fill-verify-retry for hydrated inputs). `tests/` suites now log in with demo accounts (routes require auth) - keep the `login()`/`api()` helpers when editing them. Deploy: `.github/workflows/deploy.yml` (push main -> GHCR `staging-<sha>` -> SSH staging, `STAGING_*` secrets-gated) + `.github/workflows/deploy-prod.yml` (workflow_dispatch + `environment: production` approval gate -> blue/green) + `scripts/deploy/staging.sh` + `scripts/deploy/blue-green.sh` (healthcheck-gated promotion, auto-rollback; `bash -n` + yaml-lint validated, remote-server steps need a real host). GitHub-side setup required (not configurable from the repo): branch protection requiring the four CI checks + `STAGING_*`/`PROD_*` secrets + `production` environment reviewers.
- P7 生态与集成: 见 `docs/设计说明.md` 十五·十七~十五·二十. OpenAPI/SDK (`src/lib/openapi/spec.ts` -> `GET /api/openapi.json`, Swagger UI at `/docs` via `swagger-ui-dist` copied to `public/vendor/swagger-ui/` by postinstall `scripts/tools/copy-swagger-ui.mjs` - gitignored, don't commit); versioned API surface `/api/v1/*` (me/knowledge-bases/chat/agent-run/webhooks) with API-key scope enforcement (`src/lib/apikeys/scopes.ts`, SCOPES: kb:read/kb:write/chat:read/agent:run; legacy routes unchanged = backward compat); webhook engine (`src/lib/webhooks/`, queue job `webhook-deliver`, HMAC `X-KAI-Signature`, events kb.ready/agent.completed/usage.alert, retries + dead-letter; Prisma `WebhookSubscription`); three-language SDKs in `sdk/` (js mjs+d.ts / python stdlib / go stdlib; `go test ./sdk/go` unit-testable, `scripts/smoke/test-sdk.ts` runs all three against a live server); developer portal `/developer` (nav, owner/admin/editor). Integrations (P7-2): embeddable widget `public/widget/kai-widget.js` (self-contained IIFE, no imports, deployable standalone - `test-widget-ui.mjs` verifies in Chrome), chat bots `src/lib/integrations/bots.ts` + `POST /api/v1/integrations/bot/m/<token>` (Slack/Feishu/DingTalk payload adapters + challenge echo; token stored SHA-256-hashed, shown once; `x-kai-platform` override header; answers via `askOnce` which consumes the shared SSE pipeline in-process), Chrome extension `integrations/chrome-extension/` (MV3), n8n/Zapier = v1 API + webhooks. CORS on `/api/*` (header-auth only). Per-integration rate-limit tier `integration:<id>` (`RATE_LIMIT_INTEGRATION_PER_MIN` default 120, enforced in-route - bot callbacks are in proxy SKIP_PATHS). Knowledge graph (P7-3): `src/lib/kg/` - `extract.ts` (deterministic pattern NER for zh/en: persons/orgs/concepts/events; tempered token stops org names at conjunctions/verbs - tests depend on exact patterns), `store.ts` (globalThis `__KAI_GRAPH_STORE__`, per-doc incremental index, Prisma `KnowledgeEntity`/`KnowledgeRelation`), `graph-rag.ts` (1-hop neighbor expansion boosts chunks mentioning NEIGHBOR labels only; wired into `ask.ts` when `kb.settings.graphRag !== false`); graph API `/api/knowledge-base/[id]/graph` + `/graph/search`; interactive SVG renderer `src/components/app/knowledge-graph.tsx` on top of the d3-force layout in `src/lib/kg/layout.ts` (P7-5 replaced the hand-written physics; deterministic tick(300) so SSR/client coordinates match; edges address nodes by LABEL, nodes carry `data-id`), page `/knowledge-base/[id]/graph`. Multimodal (P7-4): `src/lib/rag/vision.ts` `describeImage` (LLM content-parts or OCR+dims demo fallback), `/api/chat` body `images?: {mime,data}[]` (LLM passes content parts via `wireMessages` in `src/lib/llm/provider.ts`; demo describes+prepends), subtitle `.srt/.vtt` parsing (`DocType.subtitle`), voice `src/lib/voice/{stt,tts}.ts` + `src/hooks/use-speech-{recognition,synthesis}.ts` + `src/types/web-speech.d.ts` (mic button with interim transcript + auto-send, read-aloud on assistant bubbles; unsupported browsers hide controls). Acceptance smokes: `test-openapi.ts` 25/25, `test-sdk.ts` 9/9, `test-webhooks.ts` 30/30, `test-integrations.ts` 30/30, `test-graph-rag.ts` 20/20, `test-multimodal.ts` 15/15 (+ `test-{widget,graph,multimodal}-ui.mjs` CDP runs; run them against a dev server started WITHOUT the .env.local external-provider values - stale Turbopack chunks after big edits: restart `pnpm dev`). P3-2 OAuth (2026-08-13): Auth.js v5 (`next-auth@5.0.0-beta.32`) is the OAuth engine ONLY - the app session stays `kai-token` (guard.ts untouched). `src/lib/auth/authjs.ts` NextAuth config (providers google/github env-gated `GOOGLE_/GITHUB_CLIENT_ID|SECRET`; `GOOGLE_ISSUER`/`GITHUB_ISSUER` endpoint overrides for tests - pass URLs as STRINGS: a URL object created in app code lives in a different realm than @auth/core's bundle and `new URL(foreignRealmURL)` throws on the prod build; GitHub needs explicit authorization/token/userinfo strings because its defaults point at github.com and Auth.js overrides discovery endpoints with provider config). Sign-in kickoff: GET signin is unsupported in v5 - use `src/lib/auth/oauth-signin.ts` `oauthSignIn()` (POST csrf -> POST signin, `redirect: manual`, navigate to Location). Bridge: `GET /api/auth/oauth/bridge` reads the Auth.js JWT via `getToken({req, secret})` (secret MUST be explicit - env inference needs AUTH_SECRET set), resolves via `src/lib/auth/oauth-link.ts` (`upsertOauthUser` by providerId -> email link -> auto-create passwordless; `linkOauthToUser` bind mode when a kai-token session exists; `unlinkOauthProvider` keeps >=1 login method), mints kai-token, audits `auth.oauth_*`. OAuth logins skip app 2FA (provider MFA trusted, documented). `User.oauthLinks Json?` (migration `p3_2_oauth_links`); `createOAuthUser`/`verifyCredentials` handle null passwordHash (OAuth-only accounts can set a password via PATCH /api/auth/me without currentPassword). Unbind: `DELETE /api/auth/oauth/link?provider=`. P7-2 additions (2026-08-13): VS Code extension `integrations/vscode-extension/` (plain CommonJS, zero build; `ask.js`/`sync.js` are pure Node modules - SSE frames use the `text` field on token events, same as widget/chrome-extension; eslint ignores `integrations/**`); `POST /api/v1/knowledge-bases/[id]/documents` text-document endpoint (kb:write, 409 on duplicate name - idempotent syncs); Notion/Confluence sync `src/lib/integrations/sync/{notion,confluence,index}.ts` (env `NOTION_TOKEN`/`NOTION_API_URL`, `CONFLUENCE_BASE_URL|EMAIL|TOKEN`; routes `POST /api/v1/integrations/sync/{notion,confluence}` with kb:write + canEditKb; dedupe by document name; audit `integration.sync`; converters unit-tested in `sync.test.ts`). New acceptance smokes: `test-oauth.ts` 28/28 (mock OIDC server with jwks + RS256 id_token on :5092, :3100 instance), `test-sync.ts` 15/15 (mock Notion :5090 / Confluence :5091), `test-vscode.ts` 21/21 - all spawn their own `pnpm start -p 3100` with `AUTH_URL=http://localhost:3100` + high rate limits (NEXTAUTH_URL in .env.local pins :3000 - must be cleared in the spawn env).

- Health checks (P6-4): `src/lib/health/readiness.ts` - `checkDb` (SELECT 1 via `getDb()`, 3s Promise.race timeout - Prisma never connects by itself), `checkRedis` (fresh ioredis client, `connect()` with lazyConnect+connectTimeout 3s - ioredis types don't declare ping/call, use the typed `connect()`), `checkLlm` (GET `{baseUrl}/models`, env-level only, 5s AbortSignal). Semantic: unconfigured deps = `skipped` (demo mode stays ready), configured-but-unreachable = `degraded` -> 503. Alert state machine on globalThis `__KAI_HEALTH_STATE__`: ok->degraded transition notifies all owner/admin (in-app `securityAlert` via `notify()`) + `log.error` + `reportError`; re-alerts every 10 min while degraded; recovery notification; `resetReadinessState()` for tests. Routes: `GET /api/health` (liveness, always 200, in proxy SKIP_PATHS - probes must never be rate-limited), `GET /api/health/ready` (503 on degraded), `GET /api/health/db`. Docker HEALTHCHECK / compose / blue-green health-cmd all hit `/api/health`; `k8s/deployment.yaml` example has startup/liveness/readiness probes. NOTE: the 503 acceptance test spawns a SECOND server via production `next start` (Next dev locks the project dir) - see `test-health.ts`. Also: `src/types/optional-modules.d.ts` must NOT declare modules for real deps (bullmq/ioredis/mammoth/xlsx) - ambient declarations shadow the package types (P6-4 removed those stubs; keep only @aws-sdk + typeless pdf-parse).
- `prisma/schema.prisma` + `prisma/migrations/` - DB schema (drift-checked in CI)
- `.env.example` - full env var reference with demo-fallback docs
